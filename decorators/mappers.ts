/**
 * Message mapper to store name and func.
 */
export const MessageMapper: Map<string, any> = new Map();
/**
 * Command mapper to store command name and description
 */
export const CommandMapper: Set<{ command: string; description: string }> = new Set();
